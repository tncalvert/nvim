" Override syntax highlighting for Zig's multiline string prefix (\\)
" (There's also a zigMultilineStringPrefix, but that doesn't control the
" highlighting correctly)
highlight link zigMultilineStringDelimiter String

